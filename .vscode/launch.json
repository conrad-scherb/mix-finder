{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Frontend",
            "type": "node",
            "request": "launch",
            "preLaunchTask": "Frontend: Install dependencies",
            "cwd": "${workspaceRoot}/frontend",
            "runtimeExecutable": "yarn",
            "runtimeArgs": ["start"]
        }
    ]
}